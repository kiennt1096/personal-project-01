import React from "react";
import { Box, Typography } from "@mui/material";

const Hero = () => {
  return (
    <Box>
      <Typography>Design System for React JS</Typography>
    </Box>
  );
};

export default Hero;
